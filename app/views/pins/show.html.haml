= image_tag @pin.image.variant(resize_to_fit: [400, 600])
%h1= @pin.title
%p= @pin.description

Submitted By:
%p= @pin.user.email 

%br/
= link_to '<button type="button">Back</button>'.html_safe, root_path
= link_to '<button type="button">Edit</button>'.html_safe, edit_pin_path
= link_to '<button type="button">Delete</button>'.html_safe, pin_path, method: :delete, data: { confirm: "Are you sure" }